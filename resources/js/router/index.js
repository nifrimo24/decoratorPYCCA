import Vue from 'vue';
import VueRouter from 'vue-router';
import InitCustomization from '../components/InitCustomization'
import IndexEnvironment from '../components/IndexEnvironment'

const routes = [

]
const router = new VueRouter({
    routes
});

Vue.use(VueRouter);

export default router;
